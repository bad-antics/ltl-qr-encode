// examples/demo.ltl — runnable demo for ltl_qr_encode

import std::io
use ltl_qr_encode as lib

fn main() {
    io::println("ltl_qr_encode demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}